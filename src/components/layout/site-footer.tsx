export function SiteFooter() {
  return (
    <footer className="border-t border-white/8">
      <div className="mx-auto flex max-w-7xl flex-col gap-3 px-6 py-8 text-sm text-[var(--muted-foreground)] lg:flex-row lg:items-center lg:justify-between lg:px-8">
        <p>Upload a resume, compare it to the role, and keep every revision in one place.</p>
        <p>Clear scoring, practical suggestions, and saved analyses for your next application.</p>
      </div>
    </footer>
  );
}
