export function SiteFooter() {
  return (
    <footer className="border-t border-[var(--border-soft)]">
      <div className="mx-auto flex max-w-7xl flex-col gap-3 px-6 py-8 text-sm text-[var(--muted-foreground)] lg:flex-row lg:items-center lg:justify-between lg:px-8">
        <p>Private resume reviews, side-by-side comparisons, and practical edits before you apply.</p>
        <p>Clear scoring, cleaner copy, and faster repeat analysis for every role you target.</p>
      </div>
    </footer>
  );
}
